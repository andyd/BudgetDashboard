/**
 * Test Utils Index
 *
 * Central export point for all testing utilities.
 * Import from '@/__tests__/utils' instead of individual files.
 *
 * @example
 * ```tsx
 * import { renderWithProviders, mockBudgetItem, mockUnits } from '@/__tests__/utils';
 * ```
 */

// Re-export everything from test-utils
export * from './test-utils';

// Re-export mock data
export * from './mock-data';
